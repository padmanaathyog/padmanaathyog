# Supabase Configuration
# Required for the application to connect to your Supabase project
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Authentication (required for admin dashboard)
# This is used for server-side operations that require elevated privileges
# Do not expose this key in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Storage Configuration (for file uploads)
# The name of your storage bucket in Supabase
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=yoga-images

# Optional: Rate Limiting (to handle Supabase free tier limitations)
# Maximum number of requests per minute for API endpoints
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW_MS=60000

# Optional: Image Optimization
# Maximum file size for image uploads in bytes (default: 5MB)
MAX_IMAGE_SIZE=5242880
# Allowed image formats
ALLOWED_IMAGE_FORMATS=jpg,jpeg,png,webp

# Optional: External Blog Provider (for future integration)
# EXTERNAL_BLOG_API_URL=your-external-blog-api-url
# EXTERNAL_BLOG_API_KEY=your-external-blog-api-key
